# Canonical Knowledge Graph

JSON files in this directory are canonical graph sources for compilation.

Graph objects are immutable within a released version. Every node and edge requires
provenance. Hypotheses must never generate Question Templates directly.
