# Rule Graph

This directory contains declarative Build-Time Rule Graph sources.

Runtime executes rules only after they are compiled into a Knowledge Package.
The current safety rules are `research_only` and `unreviewed`; the Compiler
rejects them when production compilation is requested.
